# Security and Dependency Review Report (v0.2.5)

## 1. Overview
This report summarizes the research into Dependabot alerts and general security posture for the `lib-conxian-core` repository as of 2026-05-13.

## 2. Dependabot Status
- **Configuration:** `.github/dependabot.yml` is correctly configured to monitor:
  - Rust dependencies in the root directory (weekly).
  - Rust dependencies in the standalone `conxian-gateway` repository (weekly).
  - GitHub Actions (weekly).
- **Recent Actions:**
  - Linear issue **CON-519** tracked the rollout of `dependency-review.yml` across the Conxian organization.
  - A compensating control PR (#91) was opened to enforce dependency reviews on pull requests.
- **Current Blockers:** Automated enablement of built-in security features (Secret Scanning, Dependabot Security Updates) is currently returning `403 Forbidden` due to insufficient GitHub App permissions (`Administration: Read & write` and `Secret scanning alerts: Read` required).

## 3. Dependency Audit (Manual)
- **Test Status:** Verified stable via `cargo test --workspace` (17/17 tests passed).
A dry-run update identifies 20 packages with compatible updates. Key strategic SDKs are prioritized:
- **Bitcoin:** v0.32.9 (Aligned)
- **BDK:** v0.30.2 (Aligned)
- **secp256k1:** v0.31.1 (Aligned)
- **actix-web:** 4.13.0 (Aligned)
- **tokio:** 1.52.3 (Aligned)

## 4. Repository Hygiene
- **Forbidden Files:** Verified that no `.env`, `.pem`, or `.key` files are tracked.
- **Automated Checks:** `hygiene.yml` workflow is active and scans for forbidden tracked files on every push.
- **Zero Secret Egress (ZSE):** The repository has undergone a Security Boundary Audit (**CON-188**, **CON-191**) and is compliant.

## 5. Recommendations
1. **Org Admin Action:** Update `CharlieCreates` app permissions as specified in CON-519 comments to unblock native security endpoint enablement.
2. **Maintenance:** Execute `cargo update` and verify with `cargo test --workspace` to ingest compatibility updates for `wasm-bindgen`, `openssl`, and `aws-lc-rs`.
3. **Audit Cadence:** Maintain the quarterly full security audit as defined in `docs/architecture/HYGIENE_BASELINE.md`.

---
*Report generated by Jules.*
