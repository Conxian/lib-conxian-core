[advisories]
ignore = [
    "RUSTSEC-2023-0089",  # atomic-polyfill — unmaintained transitive dep via postcard → frost-core
    "RUSTSEC-2024-0384",  # instant — unmaintained transitive dep
    "RUSTSEC-2024-0388",  # fxhash — unmaintained transitive dep via sled → bdk
    "RUSTSEC-2024-0436",  # paste — unmaintained transitive dep
    "RUSTSEC-2025-0057",  # fxhash — duplicate advisory ID
    "RUSTSEC-2026-0098",  # rustls-webpki — URI name constraint bug
    "RUSTSEC-2026-0099",  # rustls-webpki — wildcard name constraint bug
    "RUSTSEC-2026-0104",  # rustls-webpki — CRL parsing panic
]
